# Lambda-specific requirements
# These are the dependencies needed for Lambda functions
# Install with: pip install -r requirements-lambda.txt -t ./lambda-package

boto3>=1.34.0
httpx>=0.25.0
python-chess>=1.999
authlib>=1.3.0
itsdangerous>=2.1.2

# Note: Some packages from requirements.txt may not be needed in Lambda
# Lambda runtime includes: json, os, time, typing, etc.






# Server
APP_NAME=LLM Duel Arena
APP_DEBUG=true
APP_SECRET_KEY=change_me_to_random_secret_key_minimum_32_characters

# Google OAuth - Required for user authentication and game history
# Setup Instructions:
# 1. Go to: https://console.cloud.google.com/apis/credentials
# 2. Create a new project or select existing
# 3. Click "Create Credentials" → "OAuth client ID"
# 4. Choose "Web application"
# 5. Add authorized redirect URI: http://localhost:8000/auth/callback
# 6. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback

# Database - SQLite by default (automatically created)
DATABASE_URL=sqlite:///./llm_duel_arena.db

# Models
DEFAULT_WHITE_MODEL=ollama:llama3.1:latest
DEFAULT_BLACK_MODEL=ollama:mistral-nemo:latest

# OpenAI (unused when using Ollama)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Anthropic (unused when using Ollama)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Local (HF Transformers) - optional
LOCAL_MODEL_ID=meta-llama/Llama-3.1-8B-Instruct
ENABLE_LOCAL_MODEL=false

# Redis (optional)
REDIS_URL=redis://localhost:6379/0

# Runtime
REQUEST_TIMEOUT_SECONDS=30
MOVE_RETRY_LIMIT=2
TOKEN_BUDGET_PER_MATCH=20000

# AWS DynamoDB Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
DYNAMODB_TABLE_NAME=llm-duel-arena-users
